apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: croplands-tiles
  namespace: croplands
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    alb.ingress.kubernetes.io/listen-ports: '[{ "HTTP": 80, "HTTPS": 443 }]'
spec:
  rules:
  - host: croplands-tiles.prod-asc.chs.usgs.gov
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: croplands-tiles
            port:
              name: http
  - host: tiles.croplands.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: croplands-tiles
            port:
              name: http
